REGISTRO DE ALTERAÇÕES

24/04/2026 - GitHub Copilot:
* Corrigi o cadastro/edição de vaga para não enviar `null` em `valor_auxilio_transporte` e `valor_bolsa` quando o usuário salva sem interagir nos campos de moeda.
* Ajustei o JavaScript das telas de vaga para sincronizar os campos ocultos no carregamento e no submit, forçando `0.00` quando vazio.
* Adicionei normalização defensiva no `VagaController` para converter valores vazios em `0` antes da validação/persistência.

20/04/2026 - GitHub Copilot:
* Adicionei o campo `nome_social_estagiario` em `tb_vagas` para o fluxo de vaga com estagiário definido.
* Atualizei cadastro e edição de vaga para permitir informar o nome social junto do nome do estagiário.
* Ajustei `VagaController` para validar/salvar o novo campo e retornar no endpoint `api.vagas.info`.
* Atualizei o modal de informações da vaga em criação/edição de termo para exibir também o nome social do estagiário.

20/04/2026 - GitHub Copilot:
* Adicionei suporte a nome social no cadastro e edição de estagiário (fluxos admin, público e AJAX).
* Incluí checkbox "Possui nome social?" com exibição condicional do campo "Nome Civil".
* Implementei a regra de persistência invertida no backend:
* Quando houver nome social, `nome_estagiario` salva o nome social e `nome_secundario` salva o nome civil.
* Quando não houver nome social, `nome_estagiario` segue normal e `nome_secundario` fica `null`.
* Atualizei também a edição do perfil do estagiário para respeitar a mesma regra visual e de salvamento.
* Criei migração para adicionar o novo campo `nome_secundario` em `tb_estagiarios`.

16/03/2026 - GitHub Copilot:
* Corrigi o fluxo de criação de alteração de termo para não distorcer `valor_bolsa` e `auxilio_transporte` quando os campos da alteração são enviados em branco.
* A normalização monetária agora só é aplicada quando o usuário realmente preenche `valor_bolsa_alteracao` e/ou `auxilio_transporte_alteracao`.
* Com isso, ao salvar alteração sem mexer nesses campos, o sistema preserva os valores originais do termo (sem converter `846.66` para `84666.00`).

09/03/2026 - GitHub Copilot:
* Corrigi o fluxo AJAX do cadastro de estagiário para reduzir erros de "Falha de rede" em produção:
* A action do formulário `create_ajax` agora usa rota relativa (`route(..., [], false)`), evitando conflito de esquema `http/https` em ambientes com proxy.
* Adicionei validação client-side de tamanho dos 3 anexos usando os limites de `upload_max_filesize` e `post_max_size` do servidor, com mensagens claras antes do envio.
* Reforcei o tratamento da resposta do `fetch` para diferenciar JSON inválido de erro de rede real.
* Mensagem de erro no `catch` agora inclui detalhe técnico (`error.message`) para facilitar diagnóstico.
* Adicionei barra de progresso de upload no overlay de carregamento para feedback em tempo real durante o envio.
* Troquei o envio para `XMLHttpRequest` com timeout, retry automático com backoff e mensagens de reconexão para redes instáveis.
* Implementei rascunho local automático (LocalStorage) para recuperar dados do formulário em falhas de conexão/recarga.
* Melhorei a exibição dos anexos com nome + tamanho e tratamento resiliente de parsing JSON em respostas inesperadas.

26/02/2026 - GitHub Copilot:
* Corrigi concorrência na inscrição pública de processos seletivos para evitar duplicidade em `numero_inscricao`.
* A criação de inscrição agora roda em transação (`DB::transaction`) com lock pessimista no processo (`lockForUpdate`) e revalidação de inscrição existente dentro da transação.
* Em envios simultâneos, o fluxo passa a responder de forma idempotente (sem erro 500), retornando a inscrição já existente quando aplicável.
* Ajustei a geração do `numero_inscricao` para usar `MAX(sequencial)+1` por processo em vez de `COUNT()+1`, evitando reaproveitamento de números quando há exclusões/lacunas no histórico.
* Adicionei retry automático no fluxo de inscrição pública quando ocorrer colisão de chave única em `numero_inscricao`.
* Corrigi a model `Ebcp` para conformidade PSR-4 (nome da classe alinhado ao nome do arquivo), removendo alerta de autoload.

19/02/2026 - GitHub Copilot:
* Corrigi o tipo de `fk_id_empresa` na migração de `tb_empresa_configuracoes` para `integer`, compatível com `tb_empresas`.

12/01/2026 - GitHub Copilot:
* Implementei módulo completo de Avaliação de Estágio
* Criado Model `Avaliacao` com relações e métodos auxiliares
* Migração de banco de dados com tabela `tb_avaliacoes`
* Service `AvaliacaoService` com lógica de negócio:
  - Geração automática de avaliações a cada 6 meses
  - Validação de termos ativos
  - Factory de criação com questões padrão
* Controller `AvaliacaoController` com CRUD completo:
  - Listagem com filtros (busca, tipo)
  - Visualização de avaliações
  - Geração de links de compartilhamento seguros
  - Página pública de resposta (sem autenticação)
  - Limpeza/reset de avaliações
  - Contador para navbar
* Views responsivas:
  - Listagem com filtros e paginação
  - Visualização de avaliações por termo
  - Formulário de resposta com validação
  - Páginas de erro e sucesso
* Job `GerarAvaliacoesAutomaticasJob` agendado diariamente às 02:00
* Kernel.php configurado para scheduled tasks
* Rotas protegidas por middleware `nivel:admin,operador`
* Rotas públicas para resposta via token único
* Navbar atualizada com botão de Avaliações e badge de contador
* Documentação completa em `AVALIACOES_README.md`
* Seeder de teste para criar avaliações fictícias

27/01/2025 - Vinicius:
* Instalei a Biblioteca para gerar PDF usando o comando "composer require barryvdh/laravel-dompdf", acompanhei o video tutorial: https://www.youtube.com/watch?v=5WbVmMabV0U.
* Criei o arquivo "resources\views\termos\gerarPdfTermo.blade.php" e comecei o html que vai servir para gerar o PDF.